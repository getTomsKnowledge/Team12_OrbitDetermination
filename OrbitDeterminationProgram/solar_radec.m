function [rightAsc_deg , dec_deg] = solar_radec(T)

    rightAsc_deg = 0;
    dec_deg = 0;

    epsilon_rad =  
    lambda_rad = 

    rightAsc_deg = atan(cosd()*sind() / cosd() );
    dec_deg = asin(sind()*sind());

end